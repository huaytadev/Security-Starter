spring:
  application:
    name: security-starter

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  h2:
    console:
      enabled: true
      path: /h2-console

  jwt:
    secret: ${JWT_SECRET:6pmouy/bdZYkUL7YNQeVg5C12bAZqc7dvljD2l4sjmE=}
    access-token-expiration: 900000      # 15 min
    refresh-token-expiration: 604800000 

server:
  port: 8080

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html